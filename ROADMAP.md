# Agent-Mem-IO 技术路线图

> 本文档记录项目的设计思路、决策过程和每次迭代的变更点。

---

## 1. 赛题核心挑战分析

### 1.1 问题本质

大模型 Agent 的长时记忆存储面临三大核心挑战：

```
┌─────────────────────────────────────────────────────────────────┐
│                    Agent Memory I/O 挑战                        │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌──────────────┐    ┌──────────────┐    ┌──────────────┐      │
│  │  极度随机读   │    │  高频实时写   │    │  内存受限    │      │
│  │  (图遍历)     │    │  (记忆更新)   │    │  (10%-20%)   │      │
│  └──────┬───────┘    └──────┬───────┘    └──────┬───────┘      │
│         │                   │                   │              │
│         ▼                   ▼                   ▼              │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │              传统 OS I/O 机制完全失效                     │  │
│  │  • Page Cache 顺序预取无效 → Page Fault 风暴             │  │
│  │  • 随机写导致写放大 → SSD 寿命下降                       │  │
│  │  • mmap 缺页中断开销巨大 → 延迟不可控                    │  │
│  └──────────────────────────────────────────────────────────┘  │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 1.2 为什么传统方案失效？

| 传统方案 | 失效原因 | 本项目解决方案 |
|----------|----------|----------------|
| `mmap` + Page Cache | 图遍历的随机访问模式导致大量缺页中断，预取完全无效 | `O_DIRECT` + 用户态 Buffer Pool |
| `pread/read` | 同步阻塞 I/O，CPU 等待磁盘延迟 | `io_uring` 异步非阻塞 |
| 顺序预取 | 图索引的"下一跳"是跳跃式访问，非顺序 | 拓扑感知预取 |
| 原地更新写 | 随机写导致 SSD 写放大 | LSM-Tree 追加写 + 后台 Compaction |

---

## 2. 核心技术路线

### 2.1 整体架构设计

```
┌─────────────────────────────────────────────────────────────────────────┐
│                         Agent Memory Storage Engine                      │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                          │
│  ┌────────────────────────────────────────────────────────────────┐    │
│  │                    API Layer (Python/LangChain Binding)         │    │
│  │  Insert() | Update() | Delete() | Search(query, k) → [ids]     │    │
│  └────────────────────────────────────────────────────────────────┘    │
│                                   │                                      │
│                                   ▼                                      │
│  ┌────────────────────────────────────────────────────────────────┐    │
│  │                      Query Processing Layer                     │    │
│  │  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐         │    │
│  │  │  Query Queue │  │  Result Merge│  │  Top-K Heap  │         │    │
│  │  └──────────────┘  └──────────────┘  └──────────────┘         │    │
│  └────────────────────────────────────────────────────────────────┘    │
│                                   │                                      │
│                                   ▼                                      │
│  ┌────────────────────────────────────────────────────────────────┐    │
│  │                    Graph Index Layer (Vamana)                  │    │
│  │  ┌──────────────────────────────────────────────────────────┐ │    │
│  │  │  In-Memory: Graph Navigation Structure (邻居ID列表)       │ │    │
│  │  │  On-SSD:    Full Vectors (128-dim float vectors)         │ │    │
│  │  └──────────────────────────────────────────────────────────┘ │    │
│  └────────────────────────────────────────────────────────────────┘    │
│                                   │                                      │
│                                   ▼                                      │
│  ┌────────────────────────────────────────────────────────────────┐    │
│  │                   I/O Optimization Layer (核心创新)             │    │
│  │                                                                  │    │
│  │  ┌─────────────────┐  ┌─────────────────┐  ┌─────────────────┐ │    │
│  │  │  Buffer Pool    │  │  io_uring       │  │  Prefetcher     │ │    │
│  │  │  Manager        │  │  Async Engine   │  │  (拓扑感知)      │ │    │
│  │  │                 │  │                 │  │                 │ │    │
│  │  │  • 2Q/Graph-Aware│  │  • SQ/CQ Ring   │  │  • 下一跳预测   │ │    │
│  │  │  • Page Eviction│  │  • Batch Submit │  │  • 流水线重叠   │ │    │
│  │  └─────────────────┘  └─────────────────┘  └─────────────────┘ │    │
│  └────────────────────────────────────────────────────────────────┘    │
│                                   │                                      │
│                                   ▼                                      │
│  ┌────────────────────────────────────────────────────────────────┐    │
│  │                    Write Path (LSM-Tree Style)                 │    │
│  │  ┌────────────┐  ┌────────────┐  ┌────────────┐  ┌──────────┐ │    │
│  │  │ MemTable   │→ │ Immutable  │→ │ SSTable    │→ │ Compaction│ │    │
│  │  │ (Active)   │  │ MemTable   │  │ (SSD)     │  │ (Background)│ │    │
│  │  └────────────┘  └────────────┘  └────────────┘  └──────────┘ │    │
│  └────────────────────────────────────────────────────────────────┘    │
│                                   │                                      │
│                                   ▼                                      │
│  ┌────────────────────────────────────────────────────────────────┐    │
│  │                    Storage Layer (O_DIRECT)                    │    │
│  │  ┌──────────────────────────────────────────────────────────┐  │    │
│  │  │  NVMe SSD (Direct Access, Bypass Page Cache)            │  │    │
│  │  │  • Vector Data Files (aligned 4KB blocks)               │  │    │
│  │  │  • Graph Index Files (neighbor lists)                    │  │    │
│  │  │  • WAL (Write-Ahead Log for durability)                  │  │    │
│  │  └──────────────────────────────────────────────────────────┘  │    │
│  └────────────────────────────────────────────────────────────────┘    │
│                                                                          │
└─────────────────────────────────────────────────────────────────────────┘
```

### 2.2 读路径优化 (Read Path)

```
查询流程 (以 Top-K 搜索为例):

1. 接收 Query 向量
2. 从入口点开始图遍历
3. 对于当前访问节点:
   ┌─────────────────────────────────────────────────────────────┐
   │  CPU 计算: 当前节点与 Query 的距离                           │
   │     ↓ (并行)                                                │
   │  I/O 预取: 通过 io_uring 异步拉取邻居节点的向量数据          │
   │     ↓                                                       │
   │  当 CPU 完成计算时，邻居数据已在 Buffer Pool 中              │
   └─────────────────────────────────────────────────────────────┘
4. 选择下一跳节点，重复步骤3
5. 返回 Top-K 结果
```

**关键创新点**:
- **拓扑感知预取**: 利用图的邻接关系，提前知道下一跳可能访问的节点
- **计算-I/O 重叠**: CPU 计算距离的同时，I/O 引擎在后台拉取数据
- **Buffer Pool 热点缓存**: 基于图访问频次的智能缓存淘汰

### 2.3 写路径优化 (Write Path)

```
写入流程 (LSM-Tree 风格):

1. 新向量写入 → MemTable (内存中的有序结构)
2. MemTable 满了 → 转为 Immutable MemTable
3. 后台线程将 Immutable MemTable 刷写到 SSD (顺序写)
4. 后台 Compaction 合并多个 SSTable，重建图索引

关键设计:
- 写入是顺序追加，避免随机写
- Compaction 在后台异步进行，不阻塞查询
- 图索引重建时保持连通性
```

---

## 3. 关键技术决策

### 3.1 为什么选择 Vamana 图索引？

| 特性 | HNSW | Vamana (DiskANN) | 本项目选择 |
|------|------|------------------|------------|
| 内存占用 | 高 (全量图在内存) | 低 (仅导航结构在内存) | ✅ Vamana |
| 随机读模式 | 极度随机 | 可预测的邻居访问 | ✅ Vamana |
| SSD 友好度 | 一般 | 专门优化 | ✅ Vamana |
| 召回率 | 高 | 高 | 相当 |

### 3.2 为什么选择 io_uring？

```
传统 I/O 模型对比:

┌─────────────────┐     ┌─────────────────┐     ┌─────────────────┐
│  同步阻塞 I/O    │     │  libaio         │     │  io_uring       │
│  (pread/pwrite) │     │  (Linux AIO)    │     │  (Linux 5.1+)   │
├─────────────────┤     ├─────────────────┤     ├─────────────────┤
│  • 系统调用开销大│     │  • 仅支持 O_DIRECT│     │  • 零系统调用   │
│  • CPU 空闲等待  │     │  • API 复杂     │     │  • 批量提交     │
│  • 无法流水线   │     │  • 不支持缓冲读  │     │  • 完成通知高效 │
│  ❌ 不适合       │     │  ⚠️ 可用但非最优 │     │  ✅ 最佳选择    │
└─────────────────┘     └─────────────────┘     └─────────────────┘
```

### 3.3 Buffer Pool 淘汰策略选择

**候选方案分析**:

1. **LRU (Least Recently Used)**
   - 优点: 实现简单
   - 缺点: 对图遍历的"扫描"模式不友好，可能淘汰热点

2. **2Q (Two Queues)**
   - 优点: 区分"扫描"和"热点"访问模式
   - 缺点: 需要调参 (两个队列的比例)

3. **Graph-Aware LRU (本项目创新)**
   - 思路: 结合图的拓扑结构，优先保留"枢纽节点" (高入度节点)
   - 实现: 维护节点的访问频次 + 入度信息
   - 预期效果: 更高的缓存命中率

**决策**: 先实现 2Q 作为基线，后续迭代优化为 Graph-Aware 策略。

---

## 4. 内存预算规划

以 SIFT1M 数据集为例 (1M 个 128 维 float 向量):

```
数据集大小: 1,000,000 × 128 × 4 bytes = 512 MB (原始向量数据)

内存限制 (10%-20%): 51.2 MB - 102.4 MB

内存分配规划:
┌────────────────────────────────────────────────────────────┐
│ 组件                    │ 大小估算        │ 占比          │
├────────────────────────────────────────────────────────────┤
│ Graph Navigation Structure │ ~20 MB       │ ~20%-40%      │
│ (邻居ID列表, 平均出度10)   │              │               │
├────────────────────────────────────────────────────────────┤
│ Buffer Pool (向量缓存)     │ ~30-60 MB    │ ~60%          │
│ (缓存约 6K-12K 向量)       │              │               │
├────────────────────────────────────────────────────────────┤
│ MemTable (写入缓冲)        │ ~5-10 MB     │ ~10%          │
├────────────────────────────────────────────────────────────┤
│ 其他运行时开销             │ ~5 MB        │ ~5%           │
└────────────────────────────────────────────────────────────┘
```

---

## 5. 风险与缓解策略

| 风险 | 可能性 | 影响 | 缓解策略 |
|------|--------|------|----------|
| Recall@10 < 85% | 中 | 致命 | 优化图索引连通性，调整搜索参数 |
| 内存超限 | 高 | 致命 | 严格限制 Buffer Pool 大小，使用 cgroups 测试 |
| I/O 毛刺 | 中 | 中等 | 实现 I/O 优先级队列，限流 Compaction |
| 图索引损坏 | 低 | 严重 | WAL 机制，定期 checkpoint |

---

## 6. 迭代历史

### 迭代 0: 项目初始化 (2026-03-29)
- 创建项目结构
- 确定技术路线
- 编写设计文档

---

## 7. 下一步行动

1. **阶段1完成**: 创建项目目录结构，配置 CMake 构建系统
2. **阶段2**: 下载 SIFT1M 数据集，编写数据加载器
3. **阶段3**: 详细问题分解，编写技术设计文档

---

*本文档将随着项目进展持续更新。*